[project]
name = "OauthApp"
dependencies = [
    "flask",
    "requests",
    "flask_sqlalchemy",
    "flask_bcrypt",
    "mysql-connector-python"
]
version = "0.1"
description = "An Oautharation project for EE368"

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"